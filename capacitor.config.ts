import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'EdFinanciera',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
